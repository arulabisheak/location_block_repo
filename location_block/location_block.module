<?php

/**
 * Implements hook_theme().
 */
function location_block_theme($existing, $type, $theme, $path) {
  return [
    'location_block' => [
      'variables' => ['country' => NULL, 'city' => NULL, 'datetime' => NULL],
    ],
  ];
}
